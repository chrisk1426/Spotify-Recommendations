# Frontend

Textual terminal UI for the Flask backend in this repository.

From the repository root, install and run with:

```bash
pip install -e ./frontend
spotify-explorer-tui
```

See the root `README.md` for full setup instructions.
